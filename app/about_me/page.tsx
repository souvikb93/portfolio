import { PagePlaceholder } from "@/components/PagePlaceholder";

export const metadata = { title: "About — Souvik B" };

export default function AboutPage() {
  return (
    <PagePlaceholder
      eyebrow="(01) About Me"
      title="Building products at the intersection of design and AI."
    />
  );
}
